# No cloud credentials are required: compute runs on the sophon GPU box over SSH
# (Ray, sudo-less) and HuggingFace is reached via hf-mirror.com. This file is
# kept as a placeholder; copy to .env only if you add secrets of your own.
