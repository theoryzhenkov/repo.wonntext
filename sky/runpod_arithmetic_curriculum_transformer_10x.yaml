# Fine-tune the 10x Transformer WikiText checkpoint on the two-stage arithmetic curriculum.
# Requires assets/weights/transformer_wikitext_10x.bin (produced by the WikiText pre-training run).
# Uses the default SkyPilot image.

name: arithmetic-curriculum-transformer-10x

resources:
  cloud: runpod
  accelerators: A40:1
  disk_size: 100

workdir: .

setup: |
  set -e
  cd "$SKYP_TASK_WORKDIR"

  pip install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cu124
  pip install --no-cache-dir -r sky/requirements-runtime.txt

  # Generate both curriculum stages on the node.
  python scripts/prepare_arithmetic_curriculum.py \
      --out_dir data/arithmetic_curriculum \
      --tokenizer_path assets/wikitext/tokenizer.json

run: |
  set -e
  cd "$SKYP_TASK_WORKDIR"

  # Stage 1: two-digit addition.
  PYTHONPATH=src python -m wonntext.train_arithmetic \
      --data_dir data/arithmetic_curriculum/stage1 \
      --checkpoint assets/weights/transformer_wikitext_10x.bin \
      --model transformer \
      --d_model 768 \
      --nhead 8 \
      --num_layers 4 \
      --d_ff 2048 \
      --gamma 0.1 \
      --epochs 50 \
      --batchsize 64 \
      --eval_batchsize 256 \
      --lr 1e-4 \
      --clip_grad_norm 1.0 \
      --eval_freq 10 \
      --device auto \
      --save_dir runs/arithmetic_curriculum_10x \
      --exp_name stage1_transformer_10x

  # Stage 2: three-number expressions with +, -, *, /.
  PYTHONPATH=src python -m wonntext.train_arithmetic \
      --data_dir data/arithmetic_curriculum/stage2 \
      --checkpoint runs/arithmetic_curriculum_10x/stage1_transformer_10x/final.pt \
      --model transformer \
      --d_model 768 \
      --nhead 8 \
      --num_layers 4 \
      --d_ff 2048 \
      --gamma 0.1 \
      --epochs 100 \
      --batchsize 64 \
      --eval_batchsize 256 \
      --lr 1e-4 \
      --clip_grad_norm 1.0 \
      --eval_freq 10 \
      --device auto \
      --save_dir runs/arithmetic_curriculum_10x \
      --exp_name stage2_transformer_10x
